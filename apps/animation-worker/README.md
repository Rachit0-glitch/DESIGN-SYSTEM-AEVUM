# @aevum/animation-worker

The Phase 10 animation worker is an in-memory validation and fixed-time evaluation boundary. It validates a canonical
timeline, evaluates it at a supplied time or progress, and returns immutable diagnostics and resolved values.

This app has no server, queue listener, start command, deployment configuration, or active Railway service.
