# Fidelity font fixtures

`Basic-Regular.ttf` is the Basic typeface from the Google Fonts repository and is redistributed under the included
SIL Open Font License 1.1 (`OFL-Basic.txt`). It is used only to validate real custom-font loading, shaping, fallback
detection, and deterministic raster measurement.
